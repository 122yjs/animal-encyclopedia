# 동물도감 54 최신 UX·개발 개선 계획

> 기준: 실제 수업/사용 중 피드백을 반영한 최신 개선 계획
> 작성 목적: 기존 개발 문서의 장기 과제와 실제 사용 중 발견된 긴급 문제를 하나의 실행 가능한 로드맵으로 통합
> 적용 대상: 초등학교 3학년 과학 「동물의 생활」 수업용 동물도감 웹앱

---

## 1. 이번 개정의 핵심 판단

기존 개선 계획은 모바일 UX, 접근성, 문서 정리, 배포 안정성 중심으로 정리되어 있었다.
그러나 실제 사용 중 지역 미션 흐름에서 학생이 다음 미션으로 자연스럽게 넘어가지 못하는 문제가 확인되었으므로, 우선순위를 다시 조정한다.

이번 개정에서 가장 시급한 문제는 다음이다.

1. 지역 미션 클리어 후 **다음 미션으로 자동 이동되지 않음**
2. “현재 미션”을 클리어했는지, 다음 지역으로 넘어갔는지 **상태가 불명확함**
3. “우리 주변” 클리어 후 “땅”으로 넘어갔지만 **지렁이만 표시됨**
4. 지역 미션 완료 후에도 **“현재 미션으로 돌아가기”만 활성화됨**
5. 지역 미션 창 진입 시 **사이드 로딩/목록 글씨가 갈색 계열로 겹쳐 보이지 않음**
6. 체크리스트가 상단에 있어 학생이 설명을 읽기 전 **무작정 클릭하고 넘어갈 가능성**이 있음

따라서 현재 단계의 개선 방향은 **기능 추가**가 아니라,
**지역 미션 진행 흐름을 수업 중 끊기지 않게 안정화하는 것**이다.

---

## 2. 최신 우선순위 요약

| 우선순위 | 구분 | 핵심 과제 | 진행 상태 |
|---|---|---|---|
| P0 | 긴급 버그 | 지역 미션 클리어 후 다음 지역 자동 이동 | 완료 |
| P0 | 긴급 버그 | 다음 미션 상태 표시 및 버튼 활성화 로직 수정 | 완료 |
| P0 | 긴급 버그 | “우리 주변 → 땅” 전환 후 동물 목록 누락 문제 수정 | 완료 |
| P0 | 긴급 UX | 지역 미션 창 글자 대비 문제 수정 | 완료 |
| P0 | 교사용 UX | 최초 실행을 지역별 동물 범위 설정 흐름으로 전환 | 완료 |
| P0 | 긴급 버그 | 사이드메뉴 수동 지역 이동 시 교사 설정 동물 표시 | 완료 |
| P0 | 교사용 UX | `질문방 설정` 진입점을 `교사용 설정`으로 변경 | 완료 |
| P0 | 배포 UX | AI 질문방/지역별 동물 범위 설정 여부에 따라 학생용 링크 분기 | 완료 |
| P0 | 교사용 UX | 설정창에서 지역별 동물 범위 설정을 AI 질문방 설정보다 먼저 노출 | 완료 |
| P0 | 교사용 UX | 기본 학생용 링크/QR과 설정 반영 링크/QR 설명 분리 | 완료 |
| P0 | 교사용 UX | AI 질문방 미사용 선택을 `AI 질문방 사용 안 함`으로 명확화 | 완료 |
| P0 | 교사용 UX | AI 질문방 연결/미사용 버튼을 on/off 선택 상태로 표시 | 완료 |
| P0 | 배포 안정화 | Pages 배포 후 이전 JS/CSS 캐시가 남지 않도록 로컬 자산 버전 지정 | 완료 |
| P0 | 긴급 UX | 체크리스트 위치를 설명 아래로 이동 | 완료 |
| P0 | 긴급 UX | 모바일 세로 퀴즈창 뒤로가기 버튼을 좌상단에 고정 | 완료 |
| P0 | 학습 UX | 관찰 체크를 설명 항목별로 분리하고 관찰 요약을 퀴즈 문항 아래로 이동 | 완료 |
| P1 | 수업 UX | 학생이 현재 단계와 다음 행동을 명확히 알 수 있게 개선 | 완료 |
| P1 | 교사용 UX | 수업 중 미션 진행 상황 확인과 안내 문구 보강 | 완료 |
| P2 | 안정화 | 모바일/태블릿 실기기 QA, 이미지 용량, meta/OG, 폰트 오류 | 부분 완료 |
| P3 | 장기 | PWA, 진행도 백업/복원, 추천 수업 세트 | 대기 |

### 진행 현황

2026-04-30 기준 `60bc691 fix: advance mission flow after regional completion` 커밋에서 P0 지역 미션 전환 흐름을 반영했다.
이후 교사용 최초 실행 흐름을 `지역별 동물 범위 설정` 중심으로 바꾸고, 학생이 미션을 완료하지 않아도 사이드메뉴에서 다른 지역을 누르면 해당 지역에 대해 교사가 설정한 동물 카드가 보이도록 보강했다.
또한 설정 진입 문구를 `교사용 설정`으로 통일하고, 설정창에서 `지역 미션 설정`을 `AI 질문방 설정`보다 먼저 보이게 했다. 두 설정은 각각 따로 할 수 있음을 안내하며, 학생용 공유 링크는 질문방 포함 여부와 관계없이 교사용 설정이 빠진 `no-question.html`을 사용한다. 아무 설정도 없으면 기본 40마리 `no-question.html` 링크, 지역별 동물 범위만 설정하면 지역 파라미터가 붙은 `no-question.html`, AI 질문방을 설정하면 `questionUrl`이 붙은 학생용 `no-question.html` 링크를 만든다.
교사용 설정에는 학생에게 맨 아래의 학생용 링크나 QR만 보내면 된다는 안내만 남기고, 반복 설명을 줄였다. `수업 전 5분 확인`은 동물 범위 확인, AI 질문방 사용 여부 선택, 학생용 링크/QR 공유의 세 단계 워크플로우로 정리했다. 공유 패널은 현재 상태에 따라 `기본 학생용 링크/QR` 또는 `설정 반영 링크/QR`로 제목과 설명을 바꾼다. AI 질문방을 연결하지 않는 선택은 `질문방 지우기`가 아니라 `AI 질문방 사용 안 함`으로 표시하여, 처음부터 질문방 없이 쓰는 경우와 이미 저장된 질문방을 해제하는 경우를 같은 선택으로 이해할 수 있게 했다. 두 선택 버튼은 현재 상태에 따라 on/off처럼 한쪽만 강조되며, 기본 QR은 아무 설정 없이 바로 배포 가능한 `no-question` 링크임을 명확히 표시한다.
배포 확인 중 HTML은 갱신됐지만 기존 `app.js` 캐시가 남아 공유 패널의 동적 문구가 늦게 바뀌는 상황을 확인했다. 이를 막기 위해 로컬 `styles.css`, `app-config.js`, `vendor/qrcode.js`, `app.js` 참조에 배포 버전 쿼리를 붙였다.
P0 체크 흐름은 한 덩어리 체크리스트가 아니라 `생김새`, `움직임`, `사는 곳` 설명 바로 아래에서 각각 확인하도록 분리했다. 학생은 관찰 설명을 읽는 자리에서 항목별로 체크한 뒤 퀴즈를 시작한다.
모바일 세로 퀴즈창에서는 진행바/음소거 HUD는 우상단에 유지하고, 상세/퀴즈 모달의 `← 뒤로가기` 버튼을 좌상단에 배치해 서로 겹치지 않게 했다. `관찰 요약 열기`는 상세 설명 하단이 아니라 퀴즈 문항 바로 아래에 접힘 패널로 배치하여, 학생이 모를 때 문항 아래에서 열고 닫을 수 있다.
P1 수업 UX는 미션 패널에 `지금 할 일` 문구를 추가하고, 학생용 첫 CTA를 `동물 관찰 시작하기`로 단순화했으며, 오답 시 노란 단서를 다시 보도록 안내한다. 교사용 설정에는 기본 주소 오공유 방지 문구와 `수업 전 5분 확인` 체크리스트를 추가했다.
P2는 실기기 검증을 제외하고 favicon, meta description, Open Graph/Twitter 공유 메타, Tailwind CDN 제거, NanumSquareRound CDN 제거, 구형 iPad용 `inert` 대체 처리, 모션 감소 대응, 로컬 이미지 manifest/detail 경로 검증을 반영했다.

검증: `node --check app.js`, 전체 계약 테스트, `npm run build:distribution` 기준으로 확인한다.

남은 확인 항목은 태블릿·전자칠판 실기기 최종 QA와 카카오톡/문자/클래스팅 실제 공유 캐시 확인이다.

---

# 3. 긴급 개선 및 QA 항목 요약

기존에 “문제 해석” 및 “개선 방향” 형식으로 작성되었던 긴급 개선 항목들은 현재 ROADMAP상 개발이 완료되었으며, 실제 수업 및 기기 환경에서의 최종 확인만 남은 상태입니다. (상세 개발 기록은 `docs/archive/roadmap_p0_details_20260430.md` 참고)

## ✅ P0-1 ~ P0-4. 지역 미션 자동 이동 및 상태 명확화
상태: 완료  
확인 커밋: 60bc691  
남은 확인: Galaxy Tab A8에서 실제 수업 흐름 재현

## ✅ P0-5. 지역 미션 창 글자 대비 문제 수정
상태: 완료  
남은 확인: 태블릿 및 전자칠판에서 활성 상태 글자 식별 가능 여부 확인

## ✅ P0-6. 관찰 체크를 설명 항목별로 분리
상태: 완료  
남은 확인: 학생이 설명을 읽지 않고 체크만 누르는지 실제 수업 관찰

## ✅ P0-7. 모바일 세로 퀴즈창 뒤로가기와 관찰 요약 위치 수정
상태: 완료  
남은 확인: 세로 화면 실기기에서 뒤로가기 버튼과 HUD 겹침 해결 확인

## 🔍 P2. 실기기 및 공유 캐시 최종 QA
상태: 확인 필요  
확인 대상: Galaxy Tab A8, 전자칠판, 카카오톡, 문자, 클래스팅

---

# 4. 지역 미션 상태 설계 재정리

현재 피드백의 핵심은 지역 미션의 상태 전환이 불명확하다는 점이다.
따라서 상태 모델을 명확히 정리해야 한다.

## 4.1 권장 상태값

```text
notStarted   = 아직 시작하지 않음
inProgress   = 진행 중
completed    = 완료됨
nextReady    = 다음 미션 시작 가능
allCompleted = 전체 완료
```

## 4.2 상태 전환 흐름

```text
미션 시작하기 클릭
notStarted → inProgress

지역 동물 조건 충족
inProgress → completed

다음 미션 시작 클릭
completed → nextReady → 다음 지역 inProgress

마지막 지역 완료
completed → allCompleted
```

## 4.3 화면별 우선 표시

| 화면 | 우선 표시해야 할 정보 |
|---|---|
| 첫 진입 | 오늘의 현재 미션 |
| 진행 중 | 현재 미션명, 남은 동물 수 |
| 완료 직후 | 완료 축하, 다음 미션 버튼 |
| 다음 이동 직후 | 새 지역명, 관찰할 동물 목록 |
| 전체 완료 | 전체 완료 축하, 도감 보기 |

---

# 5. 기존 로드맵과 통합한 최신 개선 계획

## P0. 수업 중단 방지용 긴급 안정화

### 지역 미션 흐름

- [x] 지역 클리어 후 `다음 미션 시작` 클릭 시 다음 지역으로 자동 이동
- [x] 다음 지역 이동 후 카드 목록 즉시 갱신
- [x] “우리 주변 → 땅” 이동 시 지렁이만 표시되는 문제 수정
- [x] 미션 완료 후 버튼 우선순위를 `다음 미션 시작`으로 변경
- [x] 현재 미션/완료 미션/다음 미션 상태를 문구로 명확히 표시
- [x] 새로고침 후에도 현재 미션 상태 유지

### 미션 창 가독성

- [x] 활성 상태의 글자색과 배경색 대비 개선
- [x] 갈색 계열 중복 사용 제거
- [x] 완료/진행/대기 상태를 색상과 문구로 함께 표시
- [ ] 태블릿·전자칠판에서 실제 확인

### 미션 안내 흐름

- [x] 관찰 체크를 설명 항목별로 분리
- [x] 관찰 기준을 먼저 보여 준 뒤 해당 항목 바로 아래에서 체크하도록 구성
- [x] 퀴즈 시작 버튼은 세 가지 관찰 체크 아래에 배치
- [x] 학생이 무작정 클릭하지 않도록 안내 문구 추가

---

## P1. 학생 사용성 개선

- [x] 모바일 첫 화면에서 카드 1개 이상 보이게 구성
- [x] 사이드바/미션 패널은 요약형으로 조정
- [x] 학생용 첫 CTA를 `동물 관찰 시작하기`로 단순화
- [x] 퀴즈 오답 후 재도전 안내 문구 개선
- [x] 효과음이 없어도 시각 피드백만으로 성공/실패를 알 수 있게 개선
- [x] `포켓`, `몬스터볼` 계열 표현을 자체 용어로 변경 검토

---

## P1. 교사용 운영 UX 개선

- [x] 기본 주소 오공유 방지 안내 강화
- [x] 학생에게 보낼 링크/QR만 크게 표시
- [x] MagicSchool 학생용 참여 링크 입력 안내 단순화
- [x] 기본 학생용 링크/QR과 설정 반영 링크/QR 설명 분리
- [x] AI 질문방 미사용 선택 문구 명확화
- [x] AI 질문방 연결/미사용 버튼 on/off 상태 표시
- [x] 질문방 없는 버전을 기본 수업용으로 안내
- [x] 수업 전 5분 체크리스트 문서화
- [x] 지역 미션 흐름 설명용 교사용 안내 문구 추가

---

## P2. 배포·성능 안정화

- [x] favicon 추가
- [x] meta description 추가
- [x] Open Graph 태그 추가
- [ ] 카카오톡/문자/클래스팅 공유 미리보기 확인
- [x] 폰트 로드 오류 확인
- [x] 불필요한 외부 폰트 링크 제거 또는 로컬 폰트 단순화
- [x] Tailwind CDN 차단 시 화면 깨짐 여부 확인
- [x] 이미지 번들 추가 압축
- [x] 로컬 이미지 → 원본 출처 → placeholder 폴백 구조 유지 확인

---

## P2. 접근성·호환성

- [ ] 주요 버튼, 배지, 진행도, 힌트 문구 색상 대비 확인
- [ ] 일반 텍스트 4.5:1 이상 대비 확보
- [ ] 큰 글자와 버튼 텍스트 3:1 이상 대비 확보
- [x] 구형 iPad에서 `inert` 지원 여부 확인
- [x] 필요 시 `inert` 폴리필 적용
- [x] 키보드 조작과 터치 조작 모두 확인
- [x] 모션 감소 설정 환경에서 과도한 애니메이션이 없는지 확인

---

## P3. 장기 백로그

- [ ] PWA 오프라인 캐싱 검토
- [ ] 진행도 백업/복원 코드 검토
- [ ] 교사용 추천 수업 세트 구성
- [ ] 수업 후 정리 화면 추가
- [ ] 퀴즈 재도전 대기 시간 선택 기능
- [ ] 학생별 대시보드는 개인정보와 서버 운영 문제 검토 후 보류

---

# 6. 구현 시 주의할 점

## 6.1 단순히 버튼만 추가하면 안 됨

이번 문제는 버튼 문구 문제가 아니라 상태 전환 문제다.
따라서 `다음 미션 시작` 버튼을 보이게 하는 것만으로는 부족하다.

반드시 다음이 함께 처리되어야 한다.

- 현재 지역 상태 저장
- 다음 지역 계산
- 다음 지역 상태 활성화
- 카드 목록 필터 초기화
- 카드 목록 재렌더링
- 화면 이동 또는 안내 모달 표시
- 새로고침 후 상태 복원

## 6.2 필터 중복 적용 가능성 확인

“땅” 지역에서 지렁이만 뜨는 문제는 필터 중복 가능성이 높다.
지역 이동 시 기존 검색어, 이전 필터, 수집 상태 필터가 남아 있지 않은지 확인해야 한다.

## 6.3 학생용 문구를 먼저 설계해야 함

초3 학생은 “상태 전환”을 추상적으로 이해하지 않는다.
따라서 내부 상태보다 화면 문구가 중요하다.

나쁜 문구:

```text
현재 미션으로 돌아가기
```

상황별로 더 나은 문구:

```text
우리 주변 미션을 완료했어요
다음 미션: 땅에 사는 동물
땅 미션 시작하기
땅 동물 카드를 관찰해 보세요
```

## 6.4 교사 개입을 줄이는 방향으로 설계

수업 중 교사가 계속 “다음 지역 눌러”, “뒤로 가”, “그거 말고 이거 눌러”라고 말해야 한다면 UX가 실패한 것이다.
학생이 화면만 보고 다음 행동을 알 수 있어야 한다.

---

# 7. 수업 전 QA 시나리오

## 7.1 지역 미션 기본 흐름 테스트

```text
1. 새 브라우저 또는 수업 기록 초기화 상태에서 접속
2. 첫 지역 미션 시작
3. 해당 지역 동물 카드 관찰
4. 퀴즈 완료
5. 지역 미션 클리어 확인
6. 다음 미션 시작 클릭
7. 다음 지역명 표시 확인
8. 다음 지역 동물 카드 목록 확인
9. 새로고침
10. 현재 미션이 다음 지역으로 유지되는지 확인
```

## 7.2 “우리 주변 → 땅” 집중 테스트

```text
1. 우리 주변 미션 시작
2. 우리 주변 미션 완료
3. 다음 미션 시작 클릭
4. 현재 미션이 땅으로 바뀌는지 확인
5. 땅 지역 동물 카드가 지렁이만 뜨지 않는지 확인
6. 검색창, 필터, 수집 상태가 초기화되었는지 확인
```

## 7.3 버튼 상태 테스트

```text
진행 전: 미션 시작하기
진행 중: 현재 미션으로 돌아가기
완료 후: 다음 미션 시작
마지막 완료 후: 전체 미션 완료
```

## 7.4 가독성 테스트

```text
1. 지역 미션 창 대기 상태 확인
2. 지역 미션 창 진입 상태 확인
3. 활성 지역명 읽기
4. 완료 지역명 읽기
5. 태블릿 밝기 50%에서 확인
6. 전자칠판 전체 화면에서 확인
```

## 7.5 관찰 체크와 퀴즈 요약 흐름 테스트

```text
1. 동물 상세창 열기
2. 생김새/움직임/사는 곳 설명이 먼저 보이는지 확인
3. 각 설명 항목 바로 아래에 해당 관찰 체크가 있는지 확인
4. 세 가지 체크 전에는 퀴즈 시작 버튼이 비활성화되는지 확인
5. 퀴즈 화면에서 문항 바로 아래 `관찰 요약 열기`가 보이는지 확인
6. 모바일 세로 화면에서 `← 뒤로가기`와 음소거 버튼이 겹치지 않는지 확인
```

---

# 8. 문서 정리 반영 사항

이번 변경으로 기존 문서도 함께 수정되어야 한다.

## README.md

추가할 내용:

- 지역 미션은 순차 진행 방식이라는 설명
- 수업 중 문제가 생겼을 때 확인할 항목
- 질문방 없는 버전 권장 안내
- 수업 전 5분 점검표 링크

## AGENTS.md

추가할 내용:

- 지역 미션 상태 전환 로직 수정 시 주의사항
- `no-question.html` 직접 수정 금지
- 지역 필터, 검색 필터, 수집 상태 필터 중복 적용 주의
- 미션 완료 후 버튼 우선순위 규칙

## CHANGELOG.md

추가할 내용:

```md
## 다음 릴리스 예정

### Fixed
- 지역 미션 완료 후 다음 지역으로 자동 이동되지 않는 문제
- 다음 미션 시작 후 카드 목록이 일부만 표시되는 문제
- 미션 완료 후 버튼 우선순위가 잘못 유지되는 문제
- 지역 미션 창 활성 상태에서 글자가 잘 보이지 않는 문제

### Changed
- 관찰 체크를 설명 항목별로 분리
- 퀴즈 문항 바로 아래에 관찰 요약 배치
- 현재 미션/완료 미션/다음 미션 상태 문구 명확화
```

## QA_CHECKLIST.md

추가할 내용:

- “우리 주변 → 땅” 전환 테스트
- 미션 완료 후 버튼 상태 테스트
- 활성 지역 글자 대비 테스트
- 관찰 체크 항목별 위치 테스트
- 모바일 세로 퀴즈창 뒤로가기 위치 테스트

## ROADMAP.md

이번 문서의 P0~P3 구조를 최신 기준으로 반영한다.

---

# 9. 최종 결론

이번 피드백은 단순한 디자인 개선이 아니라 **수업 진행을 막는 흐름 오류**에 가깝다.
따라서 기존 로드맵에서 장기 과제로 두었던 모바일, meta, PWA, 문서 정리보다 우선하여 다음을 먼저 처리해야 한다.

1. 지역 미션 완료 후 다음 지역으로 확실히 이동
2. 다음 지역 카드 목록이 정확히 표시
3. 완료 후 버튼 우선순위 수정
4. 현재/완료/다음 미션 상태를 학생 눈높이에 맞게 표시
5. 미션 창 글자 대비 수정
6. 관찰 체크를 설명 항목별로 분리하고 퀴즈 문항 아래에 관찰 요약 배치

이 여섯 가지가 해결되어야 실제 수업 중 교사가 개입하지 않아도 학생이 자연스럽게 다음 미션을 이어갈 수 있다.

따라서 최신 로드맵의 첫 번째 목표는 다음과 같이 정리한다.

> **목표: 학생이 지역 미션을 클리어한 뒤, 교사의 추가 설명 없이 다음 지역 미션으로 자연스럽게 이동할 수 있게 한다.**
